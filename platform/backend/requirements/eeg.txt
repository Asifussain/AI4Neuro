# EEG pipeline dependencies (SIDDHI / ADformer).
# Installed only in the API image running EEG jobs, or a dedicated EEG worker image.
# Adapted from Alzheimer-Detection/backend/requirements.txt, standardized to the
# unified numpy/pandas floors so it can coexist with the MRI stack in one worker.

-r api.txt

numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
scipy>=1.10.1
matplotlib>=3.7.0
dtaidistance>=2.3.10
einops>=0.8.0
reformer-pytorch==1.4.4

# torch is installed separately (CPU wheels) in the Dockerfile to control the index URL:
#   pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cpu
torch>=2.5.0

# PDF reports
fpdf2>=2.7.9
